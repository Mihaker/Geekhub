    <div class="container">
      <nav class="navbar navbar-expand-lg bg-body-tertiary rounded navbar-secondary-emphasis bg-secondary-emphasis shadow-lg p-3 mb-5 bg-body-tertiary rounded " aria-label="Thirteenth navbar example">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample11" aria-controls="navbarsExample11" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse d-lg-flex" id="navbarsExample11">
            <a class="navbar-brand col-lg-3 me-0" href="http://localhost:3000/">MiwaCoolSite</a>
            <ul class="navbar-nav col-lg-6 justify-content-lg-center">
            <% if author_signed_in?%>
              <li class="nav-item">
                <%= link_to 'Поділись своєю історією', new_post_path, class: "nav-link" %>
              </li>
              <% else %>
                      <%= link_to 'Поділись своєю історією', login_path, class: "nav-link" %>
              <% end %>
            </ul>
            <div class="d-lg-flex col-lg-3 justify-content-lg-end">
            <% if author_signed_in?%>
              <div class="dropdown">
                <a class="btn btn-dark dropdown-toggle"  data-bs-toggle="dropdown"">
                <%= current_user.email%>
                </a>
                
                <ul class="dropdown-menu">
                 <li><%= link_to 'Переглянути профіль', author_path(current_user), class: 'dropdown-item'%></li>
                <li><%= link_to 'Рудагувати профіль', edit_author_path(current_user), class: 'dropdown-item'%></li>

                  <hr class="dropdown-divider">

                  <li><%= link_to 'Вийти', logout_path, class: 'dropdown-item',
                  data: { turbo_method: "delete" } %></li>
                </ul>
              </div>
            <% else %>
              <div class="btn-group">
                <%= link_to 'Зареєстурватись', new_author_path, class: 'btn btn-dark' %>
                <%= link_to 'Увійти', login_path, class: 'btn btn-light' %>
              </div>
            <% end %>
            </div>
          </div>
        </div>
      </nav>
    </div>